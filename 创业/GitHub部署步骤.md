# 🚀 GitHub Pages 部署详细步骤

## 📋 准备工作

### 1. 确保已安装 Git
打开 PowerShell，检查 Git 是否已安装：
```powershell
git --version
```

如果没有安装，下载：https://git-scm.com/download/win

---

## 🔧 步骤一：创建 GitHub 仓库

### 1. 登录 GitHub
- 访问 https://github.com
- 如果没有账号，点击 "Sign up" 注册（免费）

### 2. 创建新仓库
- 点击右上角 **"+"** → **"New repository"**
- **Repository name**（仓库名）：`nz-travel-website`（或任意名称）
- **Description**（描述）：`新西兰旅游网站`
- ⚠️ **重要：必须选择 Public**（公开，GitHub Pages 免费版需要公开仓库）
- ⚠️ **不要**勾选 "Add a README file"（因为我们要上传现有文件）
- 点击 **"Create repository"**

### ⚠️ 如果仓库已经是 Private（私有）
如果你已经创建了私有仓库，需要先改为公开：
1. 进入仓库 → **Settings**
2. 向下滚动到 **"Danger Zone"**（危险区域）
3. 点击 **"Change repository visibility"**
4. 选择 **"Make public"**
5. 输入仓库名确认
6. 点击确认按钮
7. 详细步骤见：`将仓库改为公开.md`

---

## 📤 步骤二：上传文件到 GitHub

### 方法一：使用 GitHub 网页上传（最简单）

1. **在仓库页面点击 "uploading an existing file"**

2. **拖拽上传以下文件**：
   - ✅ `index.html`
   - ✅ `styles.css`
   - ✅ `script.js`
   - ✅ `README.md`（可选）
   - ✅ `图片` 文件夹（整个文件夹，包含所有图片）

3. **填写提交信息**：
   - 在页面底部 "Commit changes" 输入框填写：`Initial commit - 新西兰旅游网站`
   - 点击 **"Commit changes"**

### 方法二：使用 Git 命令行（推荐，适合后续更新）

1. **在项目文件夹打开 PowerShell**

2. **初始化 Git 仓库**：
   ```powershell
   git init
   ```

3. **添加所有文件**：
   ```powershell
   git add .
   ```

4. **提交文件**：
   ```powershell
   git commit -m "Initial commit - 新西兰旅游网站"
   ```

5. **连接到 GitHub 仓库**：
   ```powershell
   git branch -M main
   git remote add origin https://github.com/你的用户名/nz-travel-website.git
   ```
   ⚠️ 将 `你的用户名` 替换为你的 GitHub 用户名

6. **上传文件**：
   ```powershell
   git push -u origin main
   ```
   - 会提示输入 GitHub 用户名和密码（或 Personal Access Token）

---

## 🌐 步骤三：启用 GitHub Pages

⚠️ **重要：必须先完成步骤二（上传文件），否则看不到 Pages 设置选项！**

1. **进入仓库设置**
   - 在仓库页面，点击 **"Settings"**（设置）标签
   - 确保仓库中已经有文件（至少有一个提交记录）

2. **找到 Pages 设置**
   - 在左侧菜单中找到 **"Pages"**（页面）
   - 如果看不到 "Pages" 选项，说明仓库还没有文件，请先完成步骤二

3. **配置部署源**

   **情况 A：如果看到 "Source" 或 "Build and deployment" 选项：**
   - 找到 **"Source"** 或 **"Build and deployment"** 部分
   - 点击下拉菜单，选择 **"Deploy from a branch"** 或 **"Branch"**
   - **Branch**（分支）：选择 **"main"**（或 "master"）
   - **Folder**（文件夹）：选择 **"/ (root)"** 或 **"/"**
   - 点击 **"Save"**（保存）

   **情况 B：如果看到 "Build and deployment" 但没有 Source：**
   - 在 "Build and deployment" 部分
   - 找到 **"Branch"** 下拉菜单
   - 选择 **"main"**（或 "master"）
   - 选择 **"/ (root)"** 作为文件夹
   - 点击 **"Save"**

   **情况 C：如果界面完全不同（新版本 GitHub）：**
   - 可能直接显示一个开关或按钮
   - 找到 **"Enable GitHub Pages"** 或类似的开关，打开它
   - 然后选择分支为 **"main"**

4. **等待部署**
   - 页面顶部会显示：`Your site is live at https://你的用户名.github.io/nz-travel-website/`
   - ⚠️ 部署可能需要几分钟时间（通常 1-5 分钟）
   - 可以点击链接查看部署状态

5. **访问网站**
   - 部署完成后，访问：`https://你的用户名.github.io/nz-travel-website/`
   - 或者：`https://你的用户名.github.io/nz-travel-website/index.html`

---

## 🔄 后续更新网站

### 使用网页更新：
1. 在 GitHub 仓库页面，点击要修改的文件
2. 点击右上角 **"✏️"**（编辑）按钮
3. 修改内容后，点击 **"Commit changes"**

### 使用 Git 命令行更新：
```powershell
# 修改文件后，在项目文件夹执行：
git add .
git commit -m "更新网站内容"
git push
```
GitHub Pages 会自动重新部署（通常 1-2 分钟）

---

## ✅ 检查清单

部署前确保：
- [x] 所有文件已上传到 GitHub
- [x] 图片文件夹包含所有图片
- [x] 仓库设置为 Public（公开）
- [x] GitHub Pages 已启用并配置正确
- [x] 等待部署完成（查看 Settings → Pages 页面）

---

## 🎯 常见问题

### Q: 在 Pages 设置中看不到 "Source" 选项？
**A:** 可能的原因和解决方法：
1. **仓库还没有文件** - 必须先上传文件（完成步骤二）
2. **界面版本不同** - 新版本的 GitHub 可能显示为：
   - "Build and deployment" → "Branch" 下拉菜单
   - 或者直接显示分支选择器
3. **解决方法**：
   - 确保仓库中至少有一个文件（如 index.html）
   - 刷新页面
   - 查看是否有 "Branch" 或 "Deploy from a branch" 选项
   - 如果还是看不到，尝试使用下面的"方法二：使用 GitHub Actions"

### Q: 网站显示 404 错误？
**A:** 
- 检查仓库是否为 Public（公开）
- 确认 GitHub Pages 已启用
- 等待几分钟让部署完成
- 检查文件路径是否正确
- 确保 `index.html` 文件在仓库根目录

### Q: 图片不显示？
**A:**
- 确保图片文件夹已上传
- 检查 HTML 中的图片路径（应该是：`图片/xxx.jpg`）
- 确保图片文件名与 HTML 中的路径一致
- 注意文件名大小写要匹配

### Q: 如何自定义域名？
**A:**
1. 购买域名（如：namecheap.com, godaddy.com）
2. 在仓库 Settings → Pages → Custom domain 输入域名
3. 在域名服务商添加 CNAME 记录指向：`你的用户名.github.io`

### Q: 如何查看部署状态？
**A:**
- 进入仓库 → **"Actions"** 标签
- 可以看到部署历史和状态
- 在 Settings → Pages 页面顶部也会显示部署状态

---

## 🔧 方法二：使用 GitHub Actions（如果看不到 Source 选项）

如果 GitHub Pages 设置界面没有显示 Source 选项，可以使用 GitHub Actions 自动部署：

1. **在仓库中创建 `.github/workflows` 文件夹**
   - 点击仓库中的 "Add file" → "Create new file"
   - 输入路径：`.github/workflows/deploy.yml`

2. **复制以下内容到文件**：
   ```yaml
   name: Deploy to GitHub Pages

   on:
     push:
       branches:
         - main

   permissions:
     contents: read
     pages: write
     id-token: write

   jobs:
     deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v3
         - name: Setup Pages
           uses: actions/configure-pages@v3
         - name: Upload artifact
           uses: actions/upload-pages-artifact@v2
           with:
             path: '.'
         - name: Deploy to GitHub Pages
           id: deployment
           uses: actions/deploy-pages@v2
   ```

3. **提交文件**
   - 点击 "Commit new file"

4. **启用 GitHub Pages**
   - 进入 Settings → Pages
   - 在 "Source" 或 "Build and deployment" 中选择 **"GitHub Actions"**
   - 保存

5. **等待部署**
   - 进入 "Actions" 标签查看部署进度
   - 完成后访问你的网站

---

## 🎉 完成！

部署成功后，你的网站链接格式为：
```
https://你的用户名.github.io/nz-travel-website/
```

**任何人都可以通过这个链接访问你的网站！** 🌍

---

## 📝 快速命令参考

```powershell
# 初始化仓库
git init

# 添加文件
git add .

# 提交
git commit -m "描述信息"

# 连接远程仓库（首次）
git remote add origin https://github.com/用户名/仓库名.git

# 上传
git push -u origin main

# 后续更新
git add .
git commit -m "更新信息"
git push
```

